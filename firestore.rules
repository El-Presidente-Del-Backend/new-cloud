rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas temporales para diagnóstico - MUY PERMISIVAS, SOLO PARA PRUEBAS
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}